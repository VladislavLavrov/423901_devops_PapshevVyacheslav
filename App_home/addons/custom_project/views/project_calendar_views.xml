<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_project_tasks" model="ir.actions.act_window">
        <field name="name">Все задачи</field>
        <field name="res_model">project.task</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            
        </field>
    </record>

    <record id="action_task_schedule_templates" model="ir.actions.act_window">
        <field name="name">Все шаблоны расписаний</field>
        <field name="res_model">task.schedule.template</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            
        </field>
    </record>

    <record id="action_task_schedules" model="ir.actions.act_window">
        <field name="name">Все расписания</field>
        <field name="res_model">task.schedule</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            
        </field>
    </record>

    <!-- Форма задачи — адаптация под металлургию -->
    <record id="view_task_form2_inherit" model="ir.ui.view">
        <field name="name">project.task.form.inherit.metallurgy</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="priority">16</field>
        <field name="arch" type="xml">
            <page name="extra_info" position="replace"/>

            <field name="project_id" position="attributes">
                <attribute name="string">Цех / Линия</attribute>
            </field>

            <field name="project_id" position="after">
                <field name="date_start" string="Дата начала" widget="datetime"/>
            </field>
        </field>
    </record>

   

    <!-- Календарь задач -->
    <record id="view_task_calendar_custom" model="ir.ui.view">
        <field name="name">project.task.calendar.custom.metallurgy</field>
        <field name="model">project.task</field>
        <field name="type">calendar</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <calendar
                string="Производственные задачи"
                date_start="date_start"
                date_stop="date_deadline"
                color="shift_color"
                event_limit="5"
                quick_add="false"
                event_open_popup="true"
                mode="month"
                js_class="custom_project_calendar_create_series">

                <field name="date_start"/>
                <field name="project_id"/>
                <field name="production_cycle"/>
                <field name="user_ids"/>
                <field name="priority"/>
                <field name="stage_id"/>
                <field name="date_deadline"/>
            </calendar>
        </field>
    </record>

    <!-- Дополнительно: если нужно — добавляем ответственного на стадию проекта -->
    <record id="project_project_project_stage_view_form_inherited" model="ir.ui.view">
        <field name="name">project.project.stage.inherited.metallurgy</field>
        <field name="model">project.project.stage</field>
        <field name="inherit_id" ref="project.project_project_stage_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='sequence']" position="after">
                <field name="user_id" string="Ответственный за цех" />
            </xpath>
        </field>
    </record>

    <menuitem id="menu_project_configuration_poultry_root" parent="project.menu_project_config"
              name="Прочее"
              sequence="10"/>


    <menuitem id="menu_project_tasks" name="Задачи"
              parent="menu_project_configuration_poultry_root"
              action="action_project_tasks" sequence="10"/>


    <menuitem id="menu_task_schedule" name="Расписания"
              parent="menu_project_configuration_poultry_root"
              action="action_task_schedule_templates" sequence="20"/>


        <menuitem id="menu_task_schedule_templates" name="Шаблоны расписаний"
              parent="menu_project_configuration_poultry_root"
              action="action_task_schedules" sequence="30"/>

</odoo>