@model List<CalculationHistory>

@{
    ViewData["Title"] = "Данные из базы данных";
}

<div class="container">
    <h1 class="text-center">@ViewData["Title"]</h1>

    @if (Model != null && Model.Any())
    {
        <div class="table-responsive">
            <table class="table table-striped table-bordered table-hover">
                <thead class="thead-dark">
                    <tr>
                        <th>ID</th>
                        <th>1 Операнд</th>
                        <th>2 Операнд</th>
                        <th>Результат</th>
                        <th>Дата проведения операции</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.Operand1</td>
                            <td>@item.Operand2</td>
                            <td>@item.Result</td>
                            <td>@item.CreatedAt.ToString("dd.MM.yyyy")</td>
                            <td>
                                <div class="btn-group btn-group-sm" role="group">
                                    <!-- Кнопка редактирования -->
                                    <a href="@Url.Action("Edit", "Home", new { id = item.Id })" 
                                    class="btn btn-warning" 
                                    title="Редактировать">
                                        ✏️ Редактировать
                                    </a>
                                    
                                    <!-- Кнопка удаления -->
                                    <a href="@Url.Action("Delete", "Home", new { id = item.Id })" 
                                    class="btn btn-danger" 
                                    title="Удалить">
                                        🗑️ Удалить
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="alert alert-info text-center">
            <p>Нет данных для отображения</p>
        </div>
    }

    <!-- <div class="text-center mt-3">
        <a href="@Url.Action("Create")" class="btn btn-success">Добавить новую запись</a>
    </div> -->
</div>